package enumerations;

import java.io.Serializable;

/**
 * This enumeration represents the usable, unusable and reserve cells on the shipboard.
 */
public enum TypeSpace implements Serializable {
    USABLE,
    UNUSABLE,
    RESERVE
}
